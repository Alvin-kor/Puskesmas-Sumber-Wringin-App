import 'package:flutter/material.dart';
import 'package:flutter_pkm_sw/widgets/layout.dart';

class KIA extends StatelessWidget {
  const KIA({super.key});

  @override
  Widget build(BuildContext context) {
    return Layout(
      homeTitle: 'KIA',
      child: const Text('KIA Pages'),
    );
  }
}
